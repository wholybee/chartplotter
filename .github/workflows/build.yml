name: build

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

env:
  APP_NAME: hmvchartplotter
  PKG_NAME: wholybee-chartplotter
  BUILD_TYPE: Release

jobs:
  linux:
    name: linux (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    # Built in a Debian Trixie container: the retained-GPU backend needs
    # QRhiWidget (Qt >= 6.7), and Trixie ships Qt 6.8 where the stock Ubuntu
    # runners still ship Qt 6.4. GitHub falls back to /bin/sh for container jobs,
    # so force bash for the bash-only syntax used below.
    container: debian:trixie
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: amd64
            runner: ubuntu-latest
          - name: arm64
            runner: ubuntu-24.04-arm
    steps:
      - name: Install dependencies
        run: |
          set -eux
          apt-get update
          apt-get install -y \
            ca-certificates git \
            cmake ninja-build g++ pkg-config \
            qt6-base-dev qt6-base-dev-tools qt6-base-private-dev \
            qt6-shadertools-dev qt6-websockets-dev libgdal-dev \
            dpkg-dev fakeroot apt-utils

      - uses: actions/checkout@v4

      - name: Compute artifact tag
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "ARTIFACT_TAG=${GITHUB_REF_NAME}" >> "$GITHUB_ENV"
            echo "DEB_VERSION=${GITHUB_REF_NAME#v}-1" >> "$GITHUB_ENV"
          else
            echo "ARTIFACT_TAG=sha-${GITHUB_SHA::7}" >> "$GITHUB_ENV"
            echo "DEB_VERSION=0.0.0+sha${GITHUB_SHA::7}-1" >> "$GITHUB_ENV"
          fi
          echo "DEB_ARCH=$(dpkg --print-architecture)" >> "$GITHUB_ENV"

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}

      - name: Build
        run: cmake --build build --config ${{ env.BUILD_TYPE }} --parallel

      - name: Package portable tarball
        run: |
          set -euo pipefail
          ARCH="${{ matrix.name }}"
          DIST="dist/${PKG_NAME}-${ARTIFACT_TAG}-linux-${ARCH}"
          rm -rf dist
          install -d "$DIST"

          install -m 0755 "build/${APP_NAME}" "$DIST/${APP_NAME}"
          cp -a build/symbols.bin "$DIST/"
          cp -a build/rastersymbols-*.png "$DIST/" 2>/dev/null || true
          [ -d build/gdal-data ] && cp -a build/gdal-data "$DIST/"
          # Runtime plugins (and the instruments catalogue seed) live next to the exe.
          [ -d build/plugins ] && cp -a build/plugins "$DIST/"
          [ -f build/instruments.xml ] && cp -a build/instruments.xml "$DIST/"

          tar -czf "${PKG_NAME}-${ARTIFACT_TAG}-linux-${ARCH}.tar.gz" -C "dist" "${PKG_NAME}-${ARTIFACT_TAG}-linux-${ARCH}"

      - name: Package Debian .deb
        run: |
          set -euo pipefail
          PKG="${PKG_NAME}"
          STAGE="debian/${PKG}"

          rm -rf debian
          install -d \
            "$STAGE/DEBIAN" \
            "$STAGE/usr/bin" \
            "$STAGE/usr/lib/${PKG}" \
            "$STAGE/usr/share/doc/${PKG}"

          install -m 0755 "build/${APP_NAME}" "$STAGE/usr/lib/${PKG}/${APP_NAME}"
          install -m 0644 build/symbols.bin "$STAGE/usr/lib/${PKG}/symbols.bin"
          install -m 0644 build/rastersymbols-*.png "$STAGE/usr/lib/${PKG}/" 2>/dev/null || true
          [ -d build/gdal-data ] && cp -a build/gdal-data "$STAGE/usr/lib/${PKG}/gdal-data"
          # Runtime plugins (.so) load from a plugins/ folder next to the binary.
          [ -d build/plugins ] && cp -a build/plugins "$STAGE/usr/lib/${PKG}/plugins"
          [ -f build/instruments.xml ] && install -m 0644 build/instruments.xml "$STAGE/usr/lib/${PKG}/instruments.xml"
          cat > "$STAGE/usr/bin/${APP_NAME}" <<WRAPPER
          #!/bin/sh
          exec /usr/lib/${PKG}/${APP_NAME} "\$@"
          WRAPPER
          chmod 0755 "$STAGE/usr/bin/${APP_NAME}"
          [ -f README.md ] && install -m 0644 README.md "$STAGE/usr/share/doc/${PKG}/README.md" || true
          [ -f LICENSE ] && install -m 0644 LICENSE "$STAGE/usr/share/doc/${PKG}/copyright" || true

          SIZE_KB="$(du -sk "$STAGE/usr" | awk '{print $1}')"

          cat > debian/control <<CONTROL
          Source: ${PKG}
          Section: utils
          Priority: optional
          Maintainer: ${GITHUB_REPOSITORY} <noreply@github.com>
          Standards-Version: 4.6.2

          Package: ${PKG}
          Architecture: any
          Depends: \${shlibs:Depends}, \${misc:Depends}
          Description: Marine chart viewer built with Qt 6 and GDAL
           A streamlined ENC (S-57) chart viewer.
          CONTROL

          # Scan the binary and every plugin .so so plugin-only deps (e.g.
          # libQt6WebSockets6 for the Signal K plugin) are declared too.
          SHLIB_TARGETS=(-e "$STAGE/usr/lib/${PKG}/${APP_NAME}")
          for so in "$STAGE/usr/lib/${PKG}/plugins/"*.so; do
            [ -e "$so" ] && SHLIB_TARGETS+=(-e "$so")
          done
          DEPENDS="$(dpkg-shlibdeps --ignore-missing-info -O -p"${PKG}" \
            -S"$STAGE" \
            "${SHLIB_TARGETS[@]}" | sed -n 's/^shlibs:Depends=//p')"
          [ -n "$DEPENDS" ] || DEPENDS="libc6"

          # Qt loads the SQLite driver (libqsqlite.so) and the Signal K plugin's
          # WebSockets transport via dlopen / QPluginLoader at runtime, so
          # dpkg-shlibdeps can't reliably see them — declare them explicitly.
          # Skip any that shlibdeps already picked up (avoids a duplicate entry).
          for extra in libqt6sql6-sqlite libqt6websockets6; do
            case " $DEPENDS " in
              *" $extra"*) ;;
              *) DEPENDS="${DEPENDS:+$DEPENDS, }$extra" ;;
            esac
          done

          cat > "$STAGE/DEBIAN/control" <<CONTROL
          Package: ${PKG}
          Version: ${DEB_VERSION}
          Section: utils
          Priority: optional
          Architecture: ${DEB_ARCH}
          Maintainer: ${GITHUB_REPOSITORY} <noreply@github.com>
          Depends: ${DEPENDS}
          Installed-Size: ${SIZE_KB}
          Homepage: https://github.com/${GITHUB_REPOSITORY}
          Description: Marine chart viewer built with Qt 6 and GDAL
           A streamlined ENC (S-57) chart viewer.
          CONTROL

          fakeroot dpkg-deb --build "$STAGE" "${PKG}_${DEB_VERSION}_${DEB_ARCH}.deb"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-${{ matrix.name }}
          path: |
            ${{ env.PKG_NAME }}-${{ env.ARTIFACT_TAG }}-linux-${{ matrix.name }}.tar.gz
            ${{ env.PKG_NAME }}_${{ env.DEB_VERSION }}_${{ env.DEB_ARCH }}.deb

  windows:
    name: windows (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: x64
            runner: windows-latest
            msystem: MINGW64
            prefix: mingw-w64-x86_64
            mingw_bin: /mingw64/bin
          - name: arm64
            runner: windows-11-arm
            msystem: CLANGARM64
            prefix: mingw-w64-clang-aarch64
            mingw_bin: /clangarm64/bin
    steps:
      - uses: actions/checkout@v4

      - name: Set up MSYS2 (${{ matrix.msystem }})
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          update: true
          install: >-
            ${{ matrix.prefix }}-cc
            ${{ matrix.prefix }}-cmake
            ${{ matrix.prefix }}-ninja
            ${{ matrix.prefix }}-qt6-base
            ${{ matrix.prefix }}-qt6-shadertools
            ${{ matrix.prefix }}-qt6-websockets
            ${{ matrix.prefix }}-gdal
            git
            p7zip

      - name: Compute artifact tag
        shell: msys2 {0}
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "ARTIFACT_TAG=${GITHUB_REF_NAME}" >> "$GITHUB_ENV"
          else
            echo "ARTIFACT_TAG=sha-${GITHUB_SHA::7}" >> "$GITHUB_ENV"
          fi

      - name: Configure
        shell: msys2 {0}
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}

      - name: Build
        shell: msys2 {0}
        run: cmake --build build --config ${{ env.BUILD_TYPE }} --parallel

      - name: Package portable zip
        shell: msys2 {0}
        run: |
          set -euo pipefail
          ARCH="${{ matrix.name }}"
          MINGW_BIN="${{ matrix.mingw_bin }}"
          rm -rf dist "${PKG_NAME}-${ARTIFACT_TAG}-windows-${ARCH}.zip"
          mkdir -p dist

          cp -f "build/${APP_NAME}.exe" dist/
          cp -f build/symbols.bin dist/
          cp -f build/rastersymbols-*.png dist/ 2>/dev/null || true
          [ -d build/gdal-data ] && cp -a build/gdal-data dist/

          copy_deps () {
            local bin="$1"
            [ -f "$bin" ] || return 0
            # ldd exits non-zero when it can't resolve a dependency (a plugin
            # links against hmvchartplotter.exe, which isn't on its search path),
            # and grep exits non-zero when a binary has no MinGW deps. Neither
            # should abort the step under `set -euo pipefail`, so swallow the
            # pipeline status with a trailing `|| true`.
            ldd "$bin" 2>/dev/null | awk '{print $3}' | grep -E "^${MINGW_BIN}/.*\.dll$" | sort -u | while read -r dll; do
              [ -f "$dll" ] && cp -f "$dll" dist/
            done || true
          }
          copy_deps "build/${APP_NAME}.exe"

          # Runtime plugin DLLs load from plugins/ next to the exe. Plugins pull
          # in extra Qt modules the host exe doesn't link (e.g. the Signal K
          # plugin needs Qt6WebSockets/Qt6Network); CMake's plugin post-build
          # steps already stage those DLLs next to the exe in build/, so mirror
          # build/'s top-level runtime DLLs into dist/.
          #
          # We deliberately do NOT run copy_deps (ldd) on the plugins: ldd
          # resolves deps by loading the binary through the Windows loader, and a
          # plugin's unresolved import of hmvchartplotter.exe pops a modal "DLL not
          # found" dialog that hangs headless CI forever.
          if [ -d build/plugins ]; then
            mkdir -p dist/plugins
            cp -f build/plugins/*.dll dist/plugins/
            cp -f build/*.dll dist/ 2>/dev/null || true
          fi
          [ -f build/instruments.xml ] && cp -f build/instruments.xml dist/

          WDEPLOY="${MINGW_BIN}/windeployqt.exe"
          if [ -x "$WDEPLOY" ]; then
            "$WDEPLOY" --release --no-translations --no-compiler-runtime dist/${APP_NAME}.exe
          fi

          cat > dist/run.bat <<'BAT'
          @echo off
          setlocal
          set "DIR=%~dp0"
          set "PATH=%DIR%;%SystemRoot%\System32;%SystemRoot%"
          set "GDAL_DATA=%DIR%gdal-data"
          "%DIR%hmvchartplotter.exe"
          endlocal
          BAT

          7z a "${PKG_NAME}-${ARTIFACT_TAG}-windows-${ARCH}.zip" ./dist/*

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-${{ matrix.name }}
          path: ${{ env.PKG_NAME }}-${{ env.ARTIFACT_TAG }}-windows-${{ matrix.name }}.zip

  # Build that actually runs on a Raspberry Pi (64-bit Debian Trixie / Pi OS
  # Trixie). Built inside a debian:trixie container — natively on the aarch64
  # runner, no emulation — so the binary/.deb is matched to the Pi's glibc,
  # Qt 6.8 and GDAL. Trixie (Qt 6.8) is the oldest Debian that ships QRhiWidget
  # (Qt >= 6.7), which the retained-GPU chart backend requires.
  debian-trixie:
    name: debian trixie (arm64 / Raspberry Pi)
    runs-on: ubuntu-24.04-arm
    container: debian:trixie
    # The debian:trixie container has bash, but GitHub Actions falls back to
    # /bin/sh (dash) for container jobs, which breaks the bash-only syntax used
    # below ([[ ]], ${VAR::N}, set -o pipefail, arrays). Force bash for all steps.
    defaults:
      run:
        shell: bash
    steps:
      - name: Install dependencies
        run: |
          set -eux
          apt-get update
          apt-get install -y \
            ca-certificates git \
            cmake ninja-build g++ pkg-config \
            qt6-base-dev qt6-base-dev-tools qt6-base-private-dev \
            qt6-shadertools-dev qt6-websockets-dev libgdal-dev \
            dpkg-dev fakeroot

      - uses: actions/checkout@v4

      - name: Compute artifact tag
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "ARTIFACT_TAG=${GITHUB_REF_NAME}" >> "$GITHUB_ENV"
            echo "DEB_VERSION=${GITHUB_REF_NAME#v}-1~trixie" >> "$GITHUB_ENV"
          else
            echo "ARTIFACT_TAG=sha-${GITHUB_SHA::7}" >> "$GITHUB_ENV"
            echo "DEB_VERSION=0.0.0+sha${GITHUB_SHA::7}-1~trixie" >> "$GITHUB_ENV"
          fi
          echo "DEB_ARCH=$(dpkg --print-architecture)" >> "$GITHUB_ENV"

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}

      - name: Build
        run: cmake --build build --config ${{ env.BUILD_TYPE }} --parallel

      - name: Package portable tarball
        run: |
          set -euo pipefail
          DIST="dist/${PKG_NAME}-${ARTIFACT_TAG}-debian-trixie-arm64"
          rm -rf dist
          install -d "$DIST"

          install -m 0755 "build/${APP_NAME}" "$DIST/${APP_NAME}"
          cp -a build/symbols.bin "$DIST/"
          cp -a build/rastersymbols-*.png "$DIST/" 2>/dev/null || true
          [ -d build/gdal-data ] && cp -a build/gdal-data "$DIST/"
          [ -d build/plugins ] && cp -a build/plugins "$DIST/"
          [ -f build/instruments.xml ] && cp -a build/instruments.xml "$DIST/"

          tar -czf "${PKG_NAME}-${ARTIFACT_TAG}-debian-trixie-arm64.tar.gz" -C "dist" "${PKG_NAME}-${ARTIFACT_TAG}-debian-trixie-arm64"

      - name: Package Debian .deb
        run: |
          set -euo pipefail
          PKG="${PKG_NAME}"
          STAGE="debian/${PKG}"

          rm -rf debian
          install -d \
            "$STAGE/DEBIAN" \
            "$STAGE/usr/bin" \
            "$STAGE/usr/lib/${PKG}" \
            "$STAGE/usr/share/doc/${PKG}"

          install -m 0755 "build/${APP_NAME}" "$STAGE/usr/lib/${PKG}/${APP_NAME}"
          install -m 0644 build/symbols.bin "$STAGE/usr/lib/${PKG}/symbols.bin"
          install -m 0644 build/rastersymbols-*.png "$STAGE/usr/lib/${PKG}/" 2>/dev/null || true
          [ -d build/gdal-data ] && cp -a build/gdal-data "$STAGE/usr/lib/${PKG}/gdal-data"
          [ -d build/plugins ] && cp -a build/plugins "$STAGE/usr/lib/${PKG}/plugins"
          [ -f build/instruments.xml ] && install -m 0644 build/instruments.xml "$STAGE/usr/lib/${PKG}/instruments.xml"
          cat > "$STAGE/usr/bin/${APP_NAME}" <<WRAPPER
          #!/bin/sh
          exec /usr/lib/${PKG}/${APP_NAME} "\$@"
          WRAPPER
          chmod 0755 "$STAGE/usr/bin/${APP_NAME}"
          [ -f README.md ] && install -m 0644 README.md "$STAGE/usr/share/doc/${PKG}/README.md" || true
          [ -f LICENSE ] && install -m 0644 LICENSE "$STAGE/usr/share/doc/${PKG}/copyright" || true

          SIZE_KB="$(du -sk "$STAGE/usr" | awk '{print $1}')"

          # dpkg-shlibdeps reads the source package's debian/control by default.
          cat > debian/control <<CONTROL
          Source: ${PKG}
          Section: utils
          Priority: optional
          Maintainer: ${GITHUB_REPOSITORY} <noreply@github.com>
          Standards-Version: 4.6.2

          Package: ${PKG}
          Architecture: any
          Depends: \${shlibs:Depends}, \${misc:Depends}
          Description: Marine chart viewer built with Qt 6 and GDAL
           A streamlined ENC (S-57) chart viewer.
          CONTROL

          # Scan the binary and every plugin .so so plugin-only deps (e.g.
          # libQt6WebSockets6 for the Signal K plugin) are declared too.
          SHLIB_TARGETS=(-e "$STAGE/usr/lib/${PKG}/${APP_NAME}")
          for so in "$STAGE/usr/lib/${PKG}/plugins/"*.so; do
            [ -e "$so" ] && SHLIB_TARGETS+=(-e "$so")
          done
          DEPENDS="$(dpkg-shlibdeps --ignore-missing-info -O -p"${PKG}" \
            -S"$STAGE" \
            "${SHLIB_TARGETS[@]}" | sed -n 's/^shlibs:Depends=//p')"
          [ -n "$DEPENDS" ] || DEPENDS="libc6"

          # Qt loads the SQLite driver (libqsqlite.so) and the Signal K plugin's
          # WebSockets transport via dlopen / QPluginLoader at runtime, so
          # dpkg-shlibdeps can't reliably see them — declare them explicitly.
          # Skip any that shlibdeps already picked up (avoids a duplicate entry).
          for extra in libqt6sql6-sqlite libqt6websockets6; do
            case " $DEPENDS " in
              *" $extra"*) ;;
              *) DEPENDS="${DEPENDS:+$DEPENDS, }$extra" ;;
            esac
          done

          cat > "$STAGE/DEBIAN/control" <<CONTROL
          Package: ${PKG}
          Version: ${DEB_VERSION}
          Section: utils
          Priority: optional
          Architecture: ${DEB_ARCH}
          Maintainer: ${GITHUB_REPOSITORY} <noreply@github.com>
          Depends: ${DEPENDS}
          Recommends: qt6-wayland
          Installed-Size: ${SIZE_KB}
          Homepage: https://github.com/${GITHUB_REPOSITORY}
          Description: Marine chart viewer built with Qt 6 and GDAL
           A streamlined ENC (S-57) chart viewer. Built on Debian Trixie for
           64-bit Raspberry Pi / Debian arm64.
          CONTROL

          fakeroot dpkg-deb --build "$STAGE" "${PKG}_${DEB_VERSION}_${DEB_ARCH}.deb"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: debian-trixie-arm64
          path: |
            ${{ env.PKG_NAME }}-${{ env.ARTIFACT_TAG }}-debian-trixie-arm64.tar.gz
            ${{ env.PKG_NAME }}_${{ env.DEB_VERSION }}_${{ env.DEB_ARCH }}.deb

  publish:
    name: publish to GitHub Release (+ flat APT repo)
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [linux, windows, debian-trixie]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Build flat APT repository (Packages/Release)
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y dpkg-dev apt-utils

          rm -rf apt-repo
          mkdir -p apt-repo
          cp -v release-assets/linux-amd64/*.deb apt-repo/
          cp -v release-assets/linux-arm64/*.deb apt-repo/

          pushd apt-repo >/dev/null
          dpkg-scanpackages --multiversion . /dev/null > Packages
          gzip -9c Packages > Packages.gz
          apt-ftparchive release . > Release
          popd >/dev/null

      - name: Publish versioned release (tag v*)
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-assets/linux-amd64/*.tar.gz
            release-assets/linux-arm64/*.tar.gz
            release-assets/debian-trixie-arm64/*
            release-assets/windows-x64/*.zip
            release-assets/windows-arm64/*.zip
            apt-repo/*

      - name: Publish/update stable APT release (tag "apt")
        uses: softprops/action-gh-release@v2
        with:
          tag_name: apt
          name: APT repository (stable)
          target_commitish: ${{ github.sha }}
          overwrite_files: true
          files: |
            apt-repo/*
