Never commit and merge without my permission. I will tell you when to commit and merge, and I will push myself when appropriate. 

Never push to origin. I will always do that.

Build the release configuration by default. Do not build the debug configuration unless I ask.

Only edit files in your workspace.



